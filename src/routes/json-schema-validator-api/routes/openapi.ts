import { Router, Request, Response } from 'express';
const router = Router();
const privacy = { type: 'object', properties: { data_stored: { type: 'boolean' }, retention: { type: 'string' } } };
const confidence = { type: 'object', additionalProperties: { type: 'number' } };
const actions = { type: 'array', items: { type: 'string' } };
const traceFields = { trace_id: { type: 'string' }, computed_at: { type: 'string', format: 'date-time' }, success: { type: 'boolean' } };
const provenance = { type: 'object', properties: { provider: { type: 'string' }, retrieved_at: { type: 'string', format: 'date-time' }, freshness_score: { type: 'number' } } };
const chainFields = { source_provenance: provenance, cache_ttl_seconds: { type: 'integer' }, cache_recommended: { type: 'boolean' }, recommended_next_api: { type: 'string' }, recommended_next_endpoint: { type: 'string' }, automation_safe: { type: 'boolean' } };
router.get('/', (_req: Request, res: Response) => {
  res.json({ openapi: '3.1.0', info: { title: 'JSON Schema Validator API', version: '2.0.0', description: 'Validate JSON against schemas, generate schemas from samples, compare schema versions, and suggest fixes for developer and CI/CD automation agents', 'x-agent-callable': true, 'x-mcp-compatible': true, 'x-pricing': { free_tier: { requests_per_day: 1000 }, pay_per_call: { validate: '$0.001', generate: '$0.003', compare: '$0.003', 'execution-gate': '$0.001', 'schema-intelligence': '$0.005', fix: '$0.003', batch: '$0.010' } } }, servers: [{ url: 'https://orbis-apis.onrender.com/json-schema-validator' }], security: [{ ApiKeyAuth: [] }], paths: { '/validate': { post: { operationId: 'validateJson', summary: 'Validate JSON data against a schema', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['json_data', 'schema'], properties: { json_data: { type: 'object' }, schema: { type: 'object' } } } } } }, responses: { '200': { description: 'Validation result', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, is_valid: { type: 'boolean' }, errors: { type: 'array', items: { type: 'object' } }, error_count: { type: 'integer' }, ...chainFields, confidence_per_section: confidence, recommended_actions_priority_order: actions, privacy } } } } } } } }, '/generate': { post: { operationId: 'generateSchema', summary: 'Generate JSON Schema from sample data', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['json_sample'], properties: { json_sample: { type: 'object' }, schema_version: { type: 'string' }, strict: { type: 'boolean' } } } } } }, responses: { '200': { description: 'Generated schema', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, schema: { type: 'object' }, required_fields: { type: 'array', items: { type: 'string' } }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/compare': { post: { operationId: 'compareSchemas', summary: 'Compare two schemas for breaking changes', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['schema_a', 'schema_b'], properties: { schema_a: { type: 'object' }, schema_b: { type: 'object' } } } } } }, responses: { '200': { description: 'Comparison', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, compatible: { type: 'boolean' }, breaking_changes: { type: 'array', items: { type: 'object' } }, migration_complexity: { type: 'string' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/execution-gate': { post: { operationId: 'executionGate', summary: 'Execution readiness check', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['json_data'], properties: { json_data: { type: 'object' }, objective: { type: 'string' } } } } } }, responses: { '200': { description: 'Gate', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, execution_ready: { type: 'boolean' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/schema-intelligence': { post: { operationId: 'schemaIntelligence', summary: 'ONE-CALL: validate + quality check + improvements + API compatibility', 'x-one-call': true, requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['json_data'], properties: { json_data: { type: 'object' }, schema: { type: 'object' }, context: { type: 'string' } } } } } }, responses: { '200': { description: 'Schema intelligence', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, is_valid: { type: 'boolean' }, schema_quality_score: { type: 'number' }, improvement_suggestions: { type: 'array', items: { type: 'string' } }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/fix': { post: { operationId: 'fixJson', summary: 'Suggest and auto-apply fixes for invalid JSON', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['json_data'], properties: { json_data: { type: 'object' }, schema: { type: 'object' }, errors: { type: 'array', items: { type: 'object' } } } } } } }, responses: { '200': { description: 'Fix suggestions', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, fixes: { type: 'array', items: { type: 'object' } }, corrected_json: { type: 'object' }, auto_fixable: { type: 'boolean' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/batch': { post: { operationId: 'batchValidate', summary: 'Batch validate up to 20 JSON/schema pairs', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['validations'], properties: { validations: { type: 'array', items: { type: 'object' }, maxItems: 20 } } } } } }, responses: { '200': { description: 'Batch', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, batch_count: { type: 'integer' }, valid_count: { type: 'integer' }, invalid_count: { type: 'integer' }, results: { type: 'array', items: { type: 'object' } }, ...chainFields } } } } } } } } }, components: { securitySchemes: { ApiKeyAuth: { type: 'apiKey', in: 'header', name: 'X-API-Key' } } } });
});
export default router;
