import { Router, Request, Response } from 'express';
const router = Router();
const privacy = { type: 'object', properties: { data_stored: { type: 'boolean' }, retention: { type: 'string' } } };
const confidence = { type: 'object', additionalProperties: { type: 'number' } };
const actions = { type: 'array', items: { type: 'string' } };
const traceFields = { trace_id: { type: 'string' }, computed_at: { type: 'string', format: 'date-time' }, success: { type: 'boolean' } };
const provenance = { type: 'object', properties: { provider: { type: 'string' }, retrieved_at: { type: 'string', format: 'date-time' }, freshness_score: { type: 'number' } } };
const chainFields = { source_provenance: provenance, cache_ttl_seconds: { type: 'integer' }, cache_recommended: { type: 'boolean' }, recommended_next_api: { type: 'string' }, recommended_next_endpoint: { type: 'string' }, automation_safe: { type: 'boolean' } };
router.get('/', (_req: Request, res: Response) => {
  res.json({ openapi: '3.1.0', info: { title: 'Table Extraction API', version: '2.0.0', description: 'Extract tables from documents, convert to JSON/CSV, merge datasets, and analyze tabular data for document processing agents', 'x-agent-callable': true, 'x-mcp-compatible': true, 'x-pricing': { free_tier: { requests_per_day: 500 }, pay_per_call: { extract: '$0.005', 'to-json': '$0.003', 'to-csv': '$0.003', 'execution-gate': '$0.001', 'table-intelligence': '$0.008', merge: '$0.010', batch: '$0.025' } } }, servers: [{ url: 'https://orbis-apis.onrender.com/table-extraction' }], security: [{ ApiKeyAuth: [] }], paths: { '/extract': { post: { operationId: 'extractTables', summary: 'Extract tables from document text', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['document_text'], properties: { document_text: { type: 'string' }, table_index: { type: 'integer' } } } } } }, responses: { '200': { description: 'Extracted tables', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, tables: { type: 'array', items: { type: 'object' } }, table_count: { type: 'integer' }, ...chainFields, confidence_per_section: confidence, recommended_actions_priority_order: actions, privacy } } } } } } } }, '/to-json': { post: { operationId: 'tableToJson', summary: 'Convert table to structured JSON', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['table_text'], properties: { table_text: { type: 'string' }, headers: { type: 'string' } } } } } }, responses: { '200': { description: 'JSON data', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, headers: { type: 'array', items: { type: 'string' } }, data: { type: 'array', items: { type: 'object' } }, row_count: { type: 'integer' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/to-csv': { post: { operationId: 'tableToCsv', summary: 'Convert table to CSV format', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['table_text'], properties: { table_text: { type: 'string' }, delimiter: { type: 'string' } } } } } }, responses: { '200': { description: 'CSV content', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, csv_content: { type: 'string' }, row_count: { type: 'integer' }, col_count: { type: 'integer' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/execution-gate': { post: { operationId: 'executionGate', summary: 'Execution readiness check', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['document_text'], properties: { document_text: { type: 'string' }, objective: { type: 'string' } } } } } }, responses: { '200': { description: 'Gate', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, execution_ready: { type: 'boolean' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/table-intelligence': { post: { operationId: 'tableIntelligence', summary: 'ONE-CALL: extract + analyze + insights + visualization recommendations', 'x-one-call': true, requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['document_text'], properties: { document_text: { type: 'string' }, analysis_goal: { type: 'string' } } } } } }, responses: { '200': { description: 'Table intelligence', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, table_count: { type: 'integer' }, key_insights: { type: 'array', items: { type: 'string' } }, trends: { type: 'array', items: { type: 'string' } }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/merge': { post: { operationId: 'mergeTables', summary: 'Merge multiple tables on a key', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['tables'], properties: { tables: { type: 'array', items: { type: 'object' }, minItems: 2 }, merge_key: { type: 'string' } } } } } }, responses: { '200': { description: 'Merged table', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, merged_table: { type: 'object' }, tables_merged: { type: 'integer' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/batch': { post: { operationId: 'batchExtract', summary: 'Batch extract tables from up to 10 documents', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['documents'], properties: { documents: { type: 'array', items: { type: 'object' }, maxItems: 10 } } } } } }, responses: { '200': { description: 'Batch', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, batch_count: { type: 'integer' }, results: { type: 'array', items: { type: 'object' } }, ...chainFields } } } } } } } } }, components: { securitySchemes: { ApiKeyAuth: { type: 'apiKey', in: 'header', name: 'X-API-Key' } } } });
});
export default router;
