import { Router, Request, Response } from 'express';
const router = Router();
const privacy = { type: 'object', properties: { data_stored: { type: 'boolean' }, retention: { type: 'string' } } };
const confidence = { type: 'object', additionalProperties: { type: 'number' } };
const actions = { type: 'array', items: { type: 'string' } };
const traceFields = { trace_id: { type: 'string' }, computed_at: { type: 'string', format: 'date-time' }, success: { type: 'boolean' } };
const provenance = { type: 'object', properties: { provider: { type: 'string' }, retrieved_at: { type: 'string', format: 'date-time' }, freshness_score: { type: 'number' } } };
const chainFields = { source_provenance: provenance, cache_ttl_seconds: { type: 'integer' }, cache_recommended: { type: 'boolean' }, recommended_next_api: { type: 'string' }, recommended_next_endpoint: { type: 'string' }, automation_safe: { type: 'boolean' } };
router.get('/', (_req: Request, res: Response) => {
  res.json({ openapi: '3.1.0', info: { title: 'Background Removal API', version: '2.0.0', description: 'Remove or replace image backgrounds, generate segmentation masks, and enhance subjects for e-commerce, marketing, and creative automation agents', 'x-agent-callable': true, 'x-mcp-compatible': true, 'x-pricing': { free_tier: { requests_per_day: 200 }, pay_per_call: { remove: '$0.010', replace: '$0.010', mask: '$0.008', 'execution-gate': '$0.001', 'background-intelligence': '$0.008', enhance: '$0.008', batch: '$0.040' } } }, servers: [{ url: 'https://orbis-apis.onrender.com/background-removal' }], security: [{ ApiKeyAuth: [] }], paths: { '/remove': { post: { operationId: 'removeBackground', summary: 'Remove background from image', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['image_url'], properties: { image_url: { type: 'string', format: 'uri' }, output_format: { type: 'string', enum: ['png', 'webp'] }, crop_to_subject: { type: 'boolean' } } } } } }, responses: { '200': { description: 'Background removed', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, detected_subject: { type: 'string' }, output_url: { type: 'string' }, edge_quality: { type: 'string' }, ...chainFields, confidence_per_section: confidence, recommended_actions_priority_order: actions, privacy } } } } } } } }, '/replace': { post: { operationId: 'replaceBackground', summary: 'Replace background with new image or color', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['image_url'], properties: { image_url: { type: 'string' }, background_url: { type: 'string' }, background_color: { type: 'string' }, blur_background: { type: 'boolean' } } } } } }, responses: { '200': { description: 'Background replaced', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, output_url: { type: 'string' }, compositing_quality: { type: 'string' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/mask': { post: { operationId: 'generateMask', summary: 'Generate segmentation mask for subject', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['image_url'], properties: { image_url: { type: 'string' }, mask_type: { type: 'string', enum: ['foreground', 'background', 'subject'] } } } } } }, responses: { '200': { description: 'Mask generated', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, mask_url: { type: 'string' }, coverage_percent: { type: 'number' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/execution-gate': { post: { operationId: 'executionGate', summary: 'Execution readiness check', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['image_url'], properties: { image_url: { type: 'string' }, objective: { type: 'string' } } } } } }, responses: { '200': { description: 'Gate', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, execution_ready: { type: 'boolean' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/background-intelligence': { post: { operationId: 'backgroundIntelligence', summary: 'ONE-CALL: detect subject + recommend operation + quality prediction', 'x-one-call': true, requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['image_url'], properties: { image_url: { type: 'string' }, use_case: { type: 'string' } } } } } }, responses: { '200': { description: 'Background intelligence', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, detected_subject: { type: 'string' }, recommended_operation: { type: 'string' }, background_suggestions: { type: 'array', items: { type: 'string' } }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/enhance': { post: { operationId: 'enhanceSubject', summary: 'Enhance subject after background removal', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['image_url'], properties: { image_url: { type: 'string' }, enhancements: { type: 'array', items: { type: 'string', enum: ['sharpen', 'color_correct', 'shadow', 'denoise', 'upscale'] } } } } } } }, responses: { '200': { description: 'Enhanced image', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, output_url: { type: 'string' }, enhancements_applied: { type: 'array', items: { type: 'string' } }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/batch': { post: { operationId: 'batchRemove', summary: 'Batch background removal for up to 20 images', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['images'], properties: { images: { type: 'array', items: { type: 'object' }, maxItems: 20 } } } } } }, responses: { '200': { description: 'Batch', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, batch_count: { type: 'integer' }, results: { type: 'array', items: { type: 'object' } }, ...chainFields } } } } } } } } }, components: { securitySchemes: { ApiKeyAuth: { type: 'apiKey', in: 'header', name: 'X-API-Key' } } } });
});
export default router;
