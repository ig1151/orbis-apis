import { Router, Request, Response } from 'express';
const router = Router();
const privacy = { type: 'object', properties: { data_stored: { type: 'boolean' }, retention: { type: 'string' } } };
const confidence = { type: 'object', additionalProperties: { type: 'number' } };
const actions = { type: 'array', items: { type: 'string' } };
const traceFields = { trace_id: { type: 'string' }, computed_at: { type: 'string', format: 'date-time' }, success: { type: 'boolean' } };
const provenance = { type: 'object', properties: { provider: { type: 'string' }, retrieved_at: { type: 'string', format: 'date-time' }, freshness_score: { type: 'number' } } };
const chainFields = { source_provenance: provenance, cache_ttl_seconds: { type: 'integer' }, cache_recommended: { type: 'boolean' }, recommended_next_api: { type: 'string' }, recommended_next_endpoint: { type: 'string' }, automation_safe: { type: 'boolean' } };
router.get('/', (_req: Request, res: Response) => {
  res.json({ openapi: '3.1.0', info: { title: 'PDF Generator API', version: '2.0.0', description: 'Generate PDFs from templates, merge documents, optimize file size, and add watermarks for document automation and reporting agents', 'x-agent-callable': true, 'x-mcp-compatible': true, 'x-pricing': { free_tier: { requests_per_day: 200 }, pay_per_call: { generate: '$0.010', merge: '$0.008', optimize: '$0.005', 'execution-gate': '$0.001', 'pdf-intelligence': '$0.008', watermark: '$0.005', batch: '$0.040' } } }, servers: [{ url: 'https://orbis-apis.onrender.com/pdf-generator' }], security: [{ ApiKeyAuth: [] }], paths: { '/generate': { post: { operationId: 'generatePdf', summary: 'Generate PDF from content and template', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['content'], properties: { content: { type: 'object' }, template: { type: 'string' }, format: { type: 'string', enum: ['A4', 'Letter', 'Legal'] } } } } } }, responses: { '200': { description: 'PDF spec', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, document_spec: { type: 'object' }, estimated_pages: { type: 'integer' }, generation_ready: { type: 'boolean' }, ...chainFields, confidence_per_section: confidence, recommended_actions_priority_order: actions, privacy } } } } } } } }, '/merge': { post: { operationId: 'mergePdfs', summary: 'Merge multiple PDF documents', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['document_specs'], properties: { document_specs: { type: 'array', items: { type: 'object' }, minItems: 2 }, order: { type: 'array', items: { type: 'string' } } } } } } }, responses: { '200': { description: 'Merged PDF spec', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, merged_spec: { type: 'object' }, total_pages: { type: 'integer' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/optimize': { post: { operationId: 'optimizePdf', summary: 'Optimize PDF size and quality', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['document_spec'], properties: { document_spec: { type: 'object' }, optimization_goals: { type: 'array', items: { type: 'string' } } } } } } }, responses: { '200': { description: 'Optimized PDF', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, optimizations_applied: { type: 'array', items: { type: 'string' } }, reduction_percent: { type: 'number' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/execution-gate': { post: { operationId: 'executionGate', summary: 'Execution readiness check', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['content'], properties: { content: { type: 'object' }, objective: { type: 'string' } } } } } }, responses: { '200': { description: 'Gate', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, execution_ready: { type: 'boolean' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/pdf-intelligence': { post: { operationId: 'pdfIntelligence', summary: 'ONE-CALL: layout + template + style + accessibility recommendations', 'x-one-call': true, requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['content'], properties: { content: { type: 'object' }, template: { type: 'string' }, audience: { type: 'string' }, purpose: { type: 'string' } } } } } }, responses: { '200': { description: 'PDF intelligence', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, recommended_template: { type: 'string' }, layout_suggestions: { type: 'array', items: { type: 'string' } }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/watermark': { post: { operationId: 'addWatermark', summary: 'Add watermark to PDF document', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['document_spec'], properties: { document_spec: { type: 'object' }, watermark_text: { type: 'string' }, watermark_type: { type: 'string', enum: ['diagonal_text', 'header', 'footer', 'background'] } } } } } }, responses: { '200': { description: 'Watermarked PDF', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, watermark_applied: { type: 'boolean' }, watermark_config: { type: 'object' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/batch': { post: { operationId: 'batchGenerate', summary: 'Batch generate up to 10 PDFs', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['documents'], properties: { documents: { type: 'array', items: { type: 'object' }, maxItems: 10 } } } } } }, responses: { '200': { description: 'Batch', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, batch_count: { type: 'integer' }, results: { type: 'array', items: { type: 'object' } }, ...chainFields } } } } } } } } }, components: { securitySchemes: { ApiKeyAuth: { type: 'apiKey', in: 'header', name: 'X-API-Key' } } } });
});
export default router;
