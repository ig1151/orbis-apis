import { Router, Request, Response } from 'express';
const router = Router();
const privacy = { type: 'object', properties: { data_stored: { type: 'boolean' }, retention: { type: 'string' } } };
const confidence = { type: 'object', additionalProperties: { type: 'number' } };
const actions = { type: 'array', items: { type: 'string' } };
const traceFields = { trace_id: { type: 'string' }, computed_at: { type: 'string', format: 'date-time' }, success: { type: 'boolean' } };
const provenance = { type: 'object', properties: { provider: { type: 'string' }, retrieved_at: { type: 'string', format: 'date-time' }, freshness_score: { type: 'number' } } };
const chainFields = { source_provenance: provenance, cache_ttl_seconds: { type: 'integer' }, cache_recommended: { type: 'boolean' }, recommended_next_api: { type: 'string' }, recommended_next_endpoint: { type: 'string' }, automation_safe: { type: 'boolean' } };
router.get('/', (_req: Request, res: Response) => {
  res.json({ openapi: '3.1.0', info: { title: 'Signature Detection API', version: '2.0.0', description: 'Detect, verify, and locate signatures in documents and generate audit trails for contract execution and compliance agents', 'x-agent-callable': true, 'x-mcp-compatible': true, 'x-pricing': { free_tier: { requests_per_day: 300 }, pay_per_call: { detect: '$0.005', verify: '$0.005', locate: '$0.003', 'execution-gate': '$0.001', 'signature-intelligence': '$0.010', 'audit-trail': '$0.008', batch: '$0.025' } } }, servers: [{ url: 'https://orbis-apis.onrender.com/signature-detection' }], security: [{ ApiKeyAuth: [] }], paths: { '/detect': { post: { operationId: 'detectSignatures', summary: 'Detect all signatures in document', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['document_text'], properties: { document_text: { type: 'string' }, document_metadata: { type: 'object' } } } } } }, responses: { '200': { description: 'Signatures detected', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, signature_count: { type: 'integer' }, signature_blocks: { type: 'array', items: { type: 'object' } }, all_required_signed: { type: 'boolean' }, ...chainFields, confidence_per_section: confidence, recommended_actions_priority_order: actions, privacy } } } } } } } }, '/verify': { post: { operationId: 'verifySignatures', summary: 'Verify all required signatures are present', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['document_text'], properties: { document_text: { type: 'string' }, required_signers: { type: 'array', items: { type: 'string' } } } } } } }, responses: { '200': { description: 'Verification', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, all_signed: { type: 'boolean' }, missing_signatures: { type: 'array', items: { type: 'string' } }, execution_complete: { type: 'boolean' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/locate': { post: { operationId: 'locateSignatureFields', summary: 'Locate all signature fields and positions', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['document_text'], properties: { document_text: { type: 'string' } } } } } }, responses: { '200': { description: 'Fields', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, signature_fields: { type: 'array', items: { type: 'object' } }, total_fields: { type: 'integer' }, signed_count: { type: 'integer' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/execution-gate': { post: { operationId: 'executionGate', summary: 'Execution readiness check', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['document_text'], properties: { document_text: { type: 'string' }, objective: { type: 'string' } } } } } }, responses: { '200': { description: 'Gate', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, execution_ready: { type: 'boolean' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/signature-intelligence': { post: { operationId: 'signatureIntelligence', summary: 'ONE-CALL: detect + verify + compliance + recommended action', 'x-one-call': true, requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['document_text'], properties: { document_text: { type: 'string' }, required_signers: { type: 'array', items: { type: 'string' } }, document_type: { type: 'string' } } } } } }, responses: { '200': { description: 'Signature intelligence', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, execution_status: { type: 'string' }, all_signed: { type: 'boolean' }, recommended_action: { type: 'string' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/audit-trail': { post: { operationId: 'generateAuditTrail', summary: 'Generate signature audit trail for compliance', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['document_text'], properties: { document_text: { type: 'string' }, document_id: { type: 'string' } } } } } }, responses: { '200': { description: 'Audit trail', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, audit_events: { type: 'array', items: { type: 'object' } }, legally_binding_assessment: { type: 'string' }, ...chainFields, confidence_per_section: confidence, privacy } } } } } } } }, '/batch': { post: { operationId: 'batchDetect', summary: 'Batch signature detection for up to 10 documents', requestBody: { required: true, content: { 'application/json': { schema: { type: 'object', required: ['documents'], properties: { documents: { type: 'array', items: { type: 'object' }, maxItems: 10 } } } } } }, responses: { '200': { description: 'Batch', content: { 'application/json': { schema: { type: 'object', properties: { ...traceFields, batch_count: { type: 'integer' }, results: { type: 'array', items: { type: 'object' } }, ...chainFields } } } } } } } } }, components: { securitySchemes: { ApiKeyAuth: { type: 'apiKey', in: 'header', name: 'X-API-Key' } } } });
});
export default router;
